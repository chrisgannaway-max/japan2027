# Runtime. Everything the portal and the CLI need.
pyyaml>=6.0
openpyxl>=3.1            # Excel: GM templates, the mapping worksheet
pypdf>=4.0               # PDF text, fallback for pdfplumber
pdfplumber>=0.11         # layout-preserving PDF text: how the night-audit reports are read
pydantic>=2.0
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9  # file uploads
httpx>=0.27
psycopg[binary]>=3.1     # PostgreSQL, the moment DATABASE_URL is set
pyotp>=2.9               # two-step sign-in
qrcode>=7.4

# Not here on purpose: pytesseract and pillow live in requirements-invoices.txt, and the
# AI invoice reader needs `pip install anthropic`.  The vendor-invoice side is off -- Champion
# use Odoo's own bill digitization -- and a deployment should not carry, build or patch a
# dependency for something it never runs.
